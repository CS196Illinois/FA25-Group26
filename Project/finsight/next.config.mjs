/** @type {import('next').NextConfig} */
const nextConfig = {
  // The app directory is now stable in Next.js 13+, so we don't need the experimental flag
  distDir: '.next'
};

export default nextConfig;
